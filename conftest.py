# conftest.py (project root)
# Placing this file here tells pytest to add the project root to sys.path,
# so imports like `from tools import search_listings` resolve correctly
# regardless of which directory pytest is invoked from.
